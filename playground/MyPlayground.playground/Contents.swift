import UIKit

var str = "Hello, playground"


var password = "Pawel"
var characters = Array(password)
var questionedPassword = [Character]()

for i in 0..<characters.count {
    questionedPassword.append("?")
}
